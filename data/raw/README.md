# Raw Data Directory

Place public datasets here using the layout documented in `docs/DATA.md`.

This directory is intentionally empty in the open-source package except for
placeholder files. The experiment configs read data through
`${WRONG_SCI_DATA_ROOT}`, which can point here or to an external data directory.
